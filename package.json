{
  "name": "sf-demo-api",
  "version": "1.0.0",
  "private": true,
  "description": "Salesforce-style demo API for Genesys Cloud Data Actions and screen pops",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
